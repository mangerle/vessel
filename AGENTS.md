# 通用 Rust Agent 工程规范与设计准则

本文件是所有参与本项目 Rust 代码开发与生成的 AI Agent、自动化工作流及协作者必须遵循的最高规范。本规范在任何情况下均优先执行。

---

## 一、 全局工程准则与交互偏好

### 1. 语言与交互规范
- **全中文交互**：所有的回答、交流、解释以及代码中的注释必须使用中文。
- **禁止 Emoji**：代码、文档以及交互提示中严禁使用任何 Emoji 图标。
- **日志描述**：系统输出的日志记录（Log）应使用中文进行详细、准确的描述。
- **引用导入**：除非发生命名冲突，不要使用全限定类型名，应始终优先使用 `use` 语句在文件顶部显式引入。
- **分歧处理**：对于任何不清楚、模糊、存在分歧的业务逻辑或命名，**绝对禁止猜测**，必须直接向用户询问以明确意图。

### 2. 文档操作与代码安全
- **变更预检**：在修改任意代码、配置或文档之前，必须先检查该文件当前是否存在且内容是否发生了变化。
- **确认机制**：若检测到文件已发生变化，必须将具体改动告知用户，获得明确确认后方可进行进一步修改。
- **精准手术式修改**：仅触碰必须修改的代码行，禁止进行与当前需求无关的全局格式重排或过度重构。
- **注释同步维护**：任何涉及业务逻辑、结构体字段或函数签名的代码修改，必须同步评估并更新关联注释，严禁遗留陈旧失真的注释。

### 3. Git 提交规范
- **提交标准**：提交代码时必须严格遵循 Conventional Commits 规范。
- **中文内容**：提交信息中的 `description`（简短描述）与 `body`（详细正文）必须使用中文。
- **格式示例**：`<type>(<scope>): <中文简述>`。

### 4. 核心工程哲学
- **编码前思考 (Think Before Coding)**：明确说明所有技术假设，遇到歧义时提供多维方案，逻辑不通时立即停顿并寻求澄清。
- **简洁优先 (Simplicity First)**：用最精简直接的代码解决问题，不做超前设计，不添加非必要的抽象层。
- **验证与自检 (Post-Edit Self-Review)**：
  - 正确性：改动是否切实解决了原始需求，边界条件是否覆盖。
  - 一致性：风格、命名、注释是否与周边代码保持一致。
  - 副作用：是否引入未预期的性能退化或破坏已有模块。
  - 异常路径：错误处理、空状态、异步取消与竞态是否合理闭环。

---

## 二、 Rust 通用工程与代码质量规范

本规范适用于所有 Rust 代码工程（包括核心业务库、底层驱动与应用端）。所有开发者与 Agent 必须将惯用 Rust（Idiomatic Rust）与代码健康度作为第一优先级。

### 1. 复杂度控制与重构准则
- **方法长度与单一职责**：单个常规业务逻辑函数或方法原则上控制在 50 行以内；严禁编写百行以上的巨型单体方法。但声明式路由映射表、扁平枚举的单层模式匹配（`match` 分支）以及覆盖完整场景的单元测试函数可合理放宽，杜绝为追求行数指标而人为割裂高内聚逻辑。
- **单文件行数与模块化裂变**：单个源码文件的生产业务代码原则上严格控制在 500 行以内。当单文件总行数超过 500 行（或包含测试超过 800 行）时，严禁继续堆叠逻辑，必须按关注点分离原则进行模块化目录裂变（将数据契约拆解为 `types.rs`/`models.rs`、错误枚举提取至 `error.rs`、子领域算法拆分为独立文件、长单元测试外置为独立测试模块）。
- **参数收敛（参数对象模式）**：函数或方法入参超过 4 个时，禁止继续平铺参数列表，必须重构为专用的配置/参数结构体（如 `XxxOptions`、`XxxParams`）或采用 Builder 模式。
- **长闭包扁平化**：传递给函数或异步任务的闭包如果超过 10 行或包含复杂分支逻辑，必须提取为独立的私有函数或具名方法，避免在调用链中深度嵌套。

### 2. 类型驱动设计与状态表达
- **非法状态不可表示 (Make Illegal States Unrepresentable)**：严禁在结构体中堆砌多个互斥的布尔值标志位，必须使用携带状态数据的 `enum`（状态机模式），确保编译器在编译期排除非法状态组合。
- **新类型模式 (Newtype Pattern)**：对于具有特定业务语义的原始类型（如 ID、端口号、配置标识等），优先定义元组结构体新类型（Newtype），防止传参顺序错乱与类型混淆。
- **验证前置 (Parse, Don't Validate)**：在数据进入系统边界（网络响应、外部 API、配置文件）时立即反序列化并解析为强类型，内部流转始终基于合法类型，避免在各层级反复书写松散的字符串校验。

### 3. 所有权、内存分配与性能防退化
- **杜绝滥用 `.clone()`**：严禁为了迎合借用检查器而盲目添加 `.clone()`。针对结构体、大集合与字符串，优先传递引用借用；需要跨线程或共享所有权时，明确评估并使用 `Arc`。
- **借用切片优先于拥有型容器**：函数入参优先使用 `&str` 代替 `&String`，使用 `&[T]` 代替 `&Vec<T>`，使用 `&Path` 代替 `&PathBuf`。
- **预分配容器容量 (Pre-allocation)**：在已知或可估算数据规模时，严禁裸调用 `Vec::new()` 或 `HashSet::new()`，必须使用 `with_capacity(n)` 初始化，避免在运行期频繁触发昂贵的内存重分配与元素搬移。
- **优先栈分配与小对象复用**：固定大小或小规模数据优先使用原生数组 `[T; N]`，避免无谓的堆分配；高频热点循环中优先调用 `.clear()` 复用现有容器内存，而非反复分配新容器。
- **借用写时复制 (`Cow<'a, T>`) 审慎使用**：仅在处于解析热点循环、协议编解码或大批量文本按需修改等高频性能路径时采用 `Cow<'a, str>` 或 `Cow<'a, [T]>`；普通业务数据流转中禁止随意引入显式生命周期参数过度设计。
- **避免过度嵌套的智能指针**：谨慎使用多层包裹的智能指针（如 `Arc<Mutex<Option<Box<T>>>>`），出现此类类型时应首先重新审视数据的所有权与生命周期架构。

### 4. 分层错误处理与契约保障
- **底层库与领域模块细粒度错误**：底层核心库与领域模块必须使用 `thiserror` 定义强类型、语义详尽的错误枚举，明确各个错误变体，供调用方精准匹配与模式解构，禁止在底层直接抛出无类型的通用字符串或宽泛错误。
- **禁止泄漏第三方不稳定错误**：底层模块对外暴露的公开错误枚举中，禁止盲目使用 `#[from]` 包装易变或大型外部 crate 的原始错误；跨边界时必须转换为本模块内聚的领域错误变体，防止底层依赖变动破坏外部调用契约。
- **错误描述附带动态上下文**：所有 `#[error("...")]` 描述必须全中文，且必须附带发生错误时的动态定位参数（如 `#[error("连接远端节点失败，主机: {host}，原因: {source}")]`），严禁使用缺少关键现场信息的笼统文本。
- **严禁双重记录原则 (No Double Logging)**：代码必须遵循“要么处理并记录，要么抛出不记录”原则。严禁在底层函数中既调用 `log::error!` 记录日志，又将 `Err(e)` 向上冒泡；此类做法会导致同一错误在调用链中被多次重复输出，污染日志流。错误日志应由最终消费/拦截该错误的顶层统一记录。
- **应用层与入口上下文溯源**：应用顶层、服务入口或消费层使用 `anyhow` 进行上下文包装（`.context(...)` / `.with_context(...)`），为排查定位提供清晰、具备上下文的中文错误调用链。
- **错误可恢复性划分与局部消化**：遇到非致命错误（如网络偶发抖动、非关键缓存未命中、可选配置缺失）时，必须在合适的业务层级通过匹配或组合子（如 `unwrap_or_default`、`unwrap_or_else`）安全消化，禁止无脑向外抛出导致整个主流程中断。
- **严禁生产路径裸 `unwrap()` / `expect()`**：非测试代码中，严禁对任何可能失败的 `Result` 或 `Option` 进行裸 `unwrap()`。必须通过 `?` 传播、模式匹配或安全降级处理。仅在有不可变数学/逻辑不变性（Invariants）保证必定成功且有详细注释说明时，才可使用 `expect("必须说明为何不可发生")`。

### 5. 结构化日志与安全可观测性
- **统一日志门面与禁止调试打印**：严禁在代码中遗留 `println!`、`eprintln!` 或 `dbg!`；统一使用 `log`（或 `tracing`）门面库（`log::info!`、`log::warn!`、`log::error!`），且所有日志文本必须使用中文清晰描述。
- **严格日志分级契约 (Level Guidelines)**：
  - `error!`：仅用于系统级严重故障、不可逆业务失败、数据完整性受损或需人工介入排查的异常情况。
  - `warn!`：用于非预期但已成功降级、容错重试或使用默认值恢复的场景；系统资源临界预警；外部依赖偶发超时。
  - `info!`：仅用于关键生命周期节点（应用启动/停止、核心配置加载就绪、网络连接建立、重要业务操作成功完成），保持低频且具备里程碑价值。
  - `debug!`：用于开发与问题排查过程中的上下文状态变迁、外部请求与响应概要快照（生产环境默认关闭）。
  - `trace!`：用于高频数据流、字节级协议报文、紧密循环内的细粒度性能与耗时跟踪。
- **严禁高频热点日志轰炸 (Anti-Log Flooding)**：严禁在常驻轮询、逐字节读取、密集数据流或高频循环体内部打印 `info!` 或更高级别日志，防止打满磁盘 I/O 或阻塞异步事件循环。
- **机密与凭证安全防泄漏 (Credential Privacy)**：严禁在任何日志输出中打印明文密码、私钥、Token、敏感鉴权 Header 或未经脱敏的用户个人隐私数据；涉及此类变量必须经过脱敏或哈希遮蔽。
- **结构化与关键上下文注入**：输出日志时应尽量附带关键业务标识（如操作目标 ID、节点地址、执行耗时 ms），避免散漫拼装无法检索的无意义字符串。

### 6. 并发原语、异步运行时与安全
- **禁止在异步上下文中执行阻塞操作**：严禁在异步运行时（如 `tokio`）的主工作线程中直接调用耗时的同步文件 IO、密集 CPU 计算或阻塞式系统调用，必须显式使用 `tokio::task::spawn_blocking` 调度至阻塞线程池。
- **并发容器选型契约与 DashMap 防死锁准则**：
  - **高并发独立键值读写**：优先选用分段锁并发映射容器 `DashMap`。`DashMap` 内部已实现基于引用计数的内建并发安全与轻量 `Clone`，通常无需在外层包裹冗余的 `Arc`；严禁在读多写多场景下裸用全局互斥的 `Arc<Mutex<HashMap>>`。
  - **严禁持有分段锁跨 `await`**：`DashMap::get()` 与 `get_mut()` 返回的分段锁 Guard（`Ref` / `RefMut`）是同步锁守卫。严禁将任何分片 Guard 暴露在 `.await` 挂起点之前（会引发整个异步运行时线程池死锁并破坏 `Send` 约束）；必须遵循“即查即放 / Clone 出内部 Arc 或轻量数据后立即 drop 守卫”原则。
  - **严禁迭代中重入修改**：严禁在调用 `iter()` 迭代遍历的同时向同一个 `DashMap` 实例写入数据，防止分段锁自死锁。
  - **跨键原子事务与全局快照**：若业务需要同时跨多个 Key 进行原子修改或获取全表一致性快照，必须使用 `tokio::sync::RwLock<HashMap<K, V>>`，并在获取多锁时遵循全局固定次序（Lock Ordering，如按 Key 字典序排序加锁），杜绝交叉死锁。
  - **极高频只读、极低频写入**：针对全局路由表、静态配置等读频次极高、写频次极低的场景，优先采用无锁 RCU 模式（如 `arc_swap::ArcSwap<T>`），实现读取零等待、写时原子替换指针。
- **完全禁用无界通道 (Strictly Forbid Unbounded Channels)**：
  - **全工程严禁使用任何形式的无界通道**（包括 `tokio::sync::mpsc::unbounded_channel` 与 `crossbeam_channel::unbounded`）。
  - **背压策略强制显式化**：所有通道必须使用有界通道（`channel(capacity)`），在声明通道的第一时间必须根据预期吞吐量明确设定容量上限，并显式设计缓冲区打满时的背压策略（阻塞反压 `send().await`、快速失败拒绝 `try_send()` 或环形覆盖丢弃旧数据），彻底杜绝内存无上限积压引发的 OOM。
- **严格区分同步轻量短锁与异步长锁**：
  - **微秒级纯内存短临界区**：临界区仅为纯内存增删改查且绝对不包含 I/O 或 `.await` 时，优先使用轻量同步锁（如 `parking_lot::Mutex` / `parking_lot::RwLock`），避免无脑使用 `tokio::sync::Mutex` 带来的额外 Future 状态机轮询与调度开销，且杜绝锁中毒。
  - **严禁跨 `await` 持有同步锁**：严禁在持有 `std::sync::MutexGuard` 或 `parking_lot::MutexGuard` 的作用域内调用 `.await`。跨 `await` 的状态保护才允许使用 `tokio::sync::Mutex`，或通过提前结束作用域确保在 `.await` 前锁已析构。
  - **最小化临界区 (Narrow Critical Sections)**：严禁持有任何互斥锁执行耗时计算、网络调用或文件 IO；必须将锁的保护范围限制在纯粹的状态变更行内，变更完成即刻释放守卫。
- **原子类型优先于重量级锁与内存序审慎**：
  - 简单的状态标志位、指标计数器优先采用 `std::sync::atomic`（如 `AtomicBool`、`AtomicUsize`），避免盲目使用互斥锁。
  - 严禁无脑全量使用 `Ordering::SeqCst`；独立计数器使用 `Ordering::Relaxed`，跨线程状态发布与消费严格使用 `Acquire-Release` 内存序对。
- **结构化并发、任务生命周期与平滑停机 (Structured Concurrency)**：
  - **严禁脱缰孤儿任务 (No Orphan Spawns)**：严禁裸调用 `tokio::spawn` 且丢弃 `JoinHandle`。派生的后台任务必须保留其 Handle，或者统一纳管到 `tokio::task::JoinSet` 中，保证任务生命周期可追踪、可等待、可中止。
  - **全局取消与平滑退场 (Graceful Shutdown)**：常驻异步循环与后台常驻监听任务必须监听全局取消令牌（如 `tokio_util::sync::CancellationToken` 或 `watch` 通道），接收到停机信号时安全完成在途事务并释放底层资源，严禁粗暴强制强杀。
  - **Panic 感知与故障隔离**：后台关键常驻任务必须在主流程中通过 `join_handle.await` 捕获返回值，主动检测 `JoinError::is_panic()`，输出告警日志并触发自愈降级。
- **取消安全性 (Cancellation Safety) 与外部 I/O 超时**：
  - **`tokio::select!` 取消安全性审计**：在 `tokio::select!` 多路复用中调用的所有异步方法，必须明确其取消安全性；若取消不安全（中断丢弃 Future 会导致数据截断或状态脱节），必须在循环外部通过 `tokio::pin!` 固定并跨轮次复用。
  - **外部 I/O 强制设限超时**：所有的网络请求、外部进程交互必须包裹 `tokio::time::timeout`，杜绝因对端套接字挂起或进程假死导致异步协程永久泄漏。
- **消息传递优先于共享状态**：跨模块并发协作优先倡导通道模式（如 `tokio::sync::mpsc` / `broadcast` / `watch`），减少跨线程共享可变状态，降低竞态与死锁风险。

### 7. 惯用特型与可见性封装
- **标准 Trait 优先实现**：领域结构体按需实现 `Debug`、`Clone`、`Default`、`PartialEq` 等特型；凡是具备合理默认值的结构体必须实现 `Default`。
- **惯用类型转换**：类型之间的显式转换优先实现 `From<T>` 或 `TryFrom<T>`，而非自创命名的临时转换方法。
- **构造器命名准则**：常规构造函数命名为 `new`，可能失败的构造器命名为 `try_new`，链式配置命名为 `with_xxx`。
- **最小权限原则**：结构体字段、方法与模块默认保持私有，优先使用 `pub(crate)` 限制在 crate 内部可见，严禁非必要的全局 `pub` 导出。

### 8. 测试驱动与质量覆盖
- **单元测试就近组织**：核心业务逻辑、状态流转和算法计算必须编写单元测试，统一放在模块底部的 `#[cfg(test)] mod tests` 中。
- **异常与边界覆盖**：测试用例不仅要覆盖正常的主流程（Happy Path），更必须覆盖边界值、非法输入以及预期的错误分支（确保返回正确的 `Err` 变体）。
- **集成测试解耦**：跨模块协作或黑盒功能测试组织在 `tests/` 目录下，仅通过公开 API 验证系统外部表现。

### 9. 文档注释、设计决策与资源安全 (RAII)
- **公共接口与关键逻辑深度注释**：所有对外公开（`pub`）的结构体、枚举、特型、函数，以及复杂私有算法，必须附带规范的 `///` 中文文档注释。注释不仅要说明输入输出与行为，更必须清晰阐明**设计原理（Why）**：
  - **实现初衷**：为什么采用当前方案而非其他替代实现？背后有哪些业务或系统层面的前提假设？
  - **核心优势**：当前方案在性能、并发安全、内存控制或维护成本上的具体优点是什么？
  - **代价与局限**：当前方案权衡（Trade-offs）掉的缺点或局限是什么（如：是否牺牲了部分扩展性、是否存在特定数据量下的性能拐点）？
- **代码与注释强一致性（同步修改机制）**：代码有改动，关联注释必须同步修改。严禁只修改逻辑而遗留陈旧、与实际行为脱节的误导性注释；若逻辑废弃，其对应注释必须同步清理或重写。
- **杜绝冗余复述型注释**：严禁书写对代码语法进行机械字面翻译的废话注释；注释应当聚焦于意图、前置假设（Invariants）与隐式约束，而非复述一眼可见的语句。
- **错误与异常契约显式声明**：凡是返回 `Result` 的公开函数，文档注释中必须包含 `/// # Errors` 章节说明失败场景；可能引发 panic 的路径必须包含 `/// # Panics` 章节明确触发条件。
- **RAII 确定性资源释放**：涉及系统资源（文件描述符、网络连接、系统锁、临时文件等）时，必须依赖 RAII 原则（实现 `Drop` trait 或利用安全守护对象），禁止依赖易被遗忘的人工关闭调用。
- **依赖审慎与代码极简化**：严禁为了轻微辅助功能随意引入大型外部 crate；优先使用 Rust 标准库与经过广泛验证的生态库；避免编写晦涩难懂的深层复杂宏，优先使用常规函数与 Trait 保持代码直观易读。

### 10. 序列化契约与防御性算术
- **外部数据防御性反序列化**：定义来自外部网络、系统 API、文件配置的反序列化结构体时，非必需字段必须标注 `#[serde(default)]`，外部状态枚举应考虑 `#[serde(other)]` 兜底变体，防止外部协议字段增删导致反序列化崩溃。
- **防范算术溢出与除零**：在处理来自未受信任的外部输入（如网络报文、用户请求、系统监控指标统计）的未校验大小、耗时、数据包计数与比率计算时，严禁裸用 `+`、`-`、`*`、`/`，必须优先使用饱和算术（`saturating_add`、`saturating_sub`）或检查算术（`checked_div`），杜绝运行时 panic 或静默回绕。内部受类型系统保障且范围明确的局部索引或常量运算不在此限。
- **栈深度控制与防爆栈**：深度不可预测的递归算法（如未知深度的 AST、目录树、图遍历）必须重构为基于堆显式栈（如 `Vec` 维护待遍历节点）的迭代遍历，防止在默认栈深度较小的线程中触发栈溢出（Stack Overflow）。

### 11. 系统安全沙箱与文件路径防穿越
- **路径防穿越安全红线 (Path Traversal Guard)**：凡涉及接收外部网络请求或用户参数拼接宿主机文件路径的逻辑，绝对禁止裸调用 `base_dir.join(user_input)`。必须在访问前进行安全规范化校验，确保解析后的规范绝对路径（Canonical Path）必须以受控的合法根沙箱目录为前缀，彻底杜绝包含 `../` 的越权逃逸与任意文件读写。
- **大文件与未知响应流式传输 (Streaming vs Buffering)**：对于安装包上传下载、应用部署包分发、JVM 堆转储（Heap Dump）以及大体积运行日志传输，严禁一次性调用 `read_to_end` 或 `bytes().await` 将未知体量的数据全量读入内存缓冲；必须统一采用基于流的接口（如 `tokio::io::AsyncRead`、`StreamExt` 分块读取）进行流式转发，彻底消除瞬时爆内存与 OOM 隐患。

### 12. 系统进程守护、生命周期与连接池契约
- **子进程状态收割与防僵尸进程 (Zombie Process Harvesting)**：在拉起外部系统子进程（如 Shell 运维脚本、`tail -f`、`jcmd`/`jstack` 工具）时，必须建立明确的退出等待与状态回收机制（调用 `.wait().await`），杜绝系统进程控制块（PCB）残留导致僵尸进程堆积；在宿主机连接中断、任务被取消或父进程退出时，常驻子进程必须被强制销毁（`.kill().await`），严禁遗留脱缰孤儿进程。
- **网络客户端全局单例与连接池复用 (Connection Pooling)**：`reqwest::Client` 底层自带连接池与 `Arc` 封装，全工程严禁在请求处理函数内局部临时创建 `Client::new()`；必须作为全局或长生命周期单例持有复用，充分利用 TCP 连接池、Keep-Alive 复用与本地 DNS 缓存，杜绝高频调用耗尽系统文件描述符（FD）与端口堆积。

---

## 三、 自动化质量门禁与自检清单 (Quality Gates & Checklist)

### 1. 自动化验证命令门禁 (Zero-Warning Policy)
在向用户汇报修改完成之前，必须在终端依次执行以下命令并通过验证，严禁带警告交付（若代码位于子目录或 Workspace 成员 crate 中，需切换至对应目录或追加 -p <crate-name> / --manifest-path）：
1. **代码格式化**：执行 `cargo fmt --all`（或 `cargo fmt --check`），确保代码风格符合统一格式规范。
2. **静态代码分析**：执行 `cargo clippy --all-targets -- -D warnings`，必须做到**零警告**通过，严禁随意添加 `#[allow(...)]` 绕过检查。
3. **编译与语法检查**：执行 `cargo check --all-targets` 确保编译无误。
4. **功能与回归测试**：若改动涉及核心业务逻辑、状态转换或数据处理，必须执行 `cargo test` 确保所有单元测试与集成测试全部通过。

### 2. 代码自检逐项核对清单 (Self-Review Checklist)
在每次完成代码修改前，必须逐项对照以下清单进行强制自检：
1. **[语言与规范]** 是否遵循全中文交互、中文注释、中文日志？代码与文档中是否彻底杜绝了任何 Emoji 图标？
2. **[导入与路径]** 是否遵循引用导入规范，优先在顶部使用 `use` 引入，避免了非必要的多层全限定类型名？
3. **[API 真实性]** 是否核对过实际库签名，杜绝了虚构方法或错误字段调用？
4. **[复杂度控制]** 单文件生产代码是否超标（>500 行）未进行模块化裂变？是否存在过长函数（>50 行）？参数是否超过 4 个未抽象为配置结构体？复杂长闭包是否已提取为独立具名函数？
5. **[错误契约]** 生产代码中是否完全杜绝了裸 `unwrap()` / `expect()`？是否遵循了“要么记录要么抛出”的禁止双重记录原则？错误描述是否包含动态中文上下文？
6. **[日志规范]** 日志级别选用是否严谨（杜绝热点循环打 `info!`）？是否彻底杜绝了 `println!`/`eprintln!`/`dbg!`？日志中是否绝对不存在任何明文凭证或私钥？
7. **[所有权与内存]** 是否避免了盲目 `.clone()`？已知容量的集合是否使用了 `with_capacity` 预分配？小对象是否避免了无谓的堆分配？
8. **[并发与异步]** 是否彻底禁用了无界通道？是否避免了在异步中执行阻塞操作？是否避免了跨 `await` 持有同步锁或 `DashMap` 分片 Guard？并发容器选型是否合理？外部 I/O 是否配置了超时？常驻任务是否接入了平滑停机与 panic 隔离？
9. **[防御性与契约]** 外部数据反序列化是否配置了默认值兜底？外部数值运算是否采用了饱和/检查算术（防溢出与除零）？
10. **[安全与沙箱]** 是否防范了外部输入的文件路径穿越（校验 Canonical Path 受控前缀）？大文件与未知响应传输是否采用了流式传输而非全量内存缓冲（防 OOM）？
11. **[进程与资源]** 派生的系统子进程是否具备状态退出收割与退出强杀清理机制（防僵尸与孤儿进程）？网络客户端 `reqwest::Client` 是否做到了全局单例连接池复用？
12. **[测试与文档]** 核心逻辑与错误分支是否编写了单元测试？公开接口与关键算法是否写明了为什么这么实现、优缺点与权衡？代码改动后关联注释是否已同步更新且杜绝了无意义的复述？
13. **[静态质量门禁]** 是否已执行 `cargo fmt` 并验证 `cargo clippy` 达到零警告？
